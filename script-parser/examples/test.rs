fn main() {
    // let text = std::fs::read_to_string("scripts.inc").unwrap();
    // match script_parser::inc::parse(&text) {
    //     Ok(script) => println!("{:#?}", script),
    //     Err(err) => eprintln!("Could not parse script with error {}", err),
    // }
    // let text = std::fs::read_to_string("text.inc").unwrap();
    // match script_parser::inc::parse_message_script(&text) {
    //     Ok(script) => println!("{:#?}", script),
    //     Err(err) => eprintln!("Could not parse script with error {}", err),
    // }
    // let text = include_str!("trainer_parties.h");
    // println!("{:#?}", script_parser::trainer::party::parse_parties(text));
    // let text = include_str!("trainers.h");
    // println!("{:#?}", script_parser::trainer::parse_trainers(text));
}
